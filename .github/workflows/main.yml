name: Sync Fork

on:
  schedule:
    # 每天凌晨 0 点（UTC 时间）自动运行一次
    - cron: '0 0 * * *'
  workflow_dispatch: # 允许你随时在网页上点击按钮手动触发同步

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout target repo
        uses: actions/checkout@v4

      - name: Sync upstream
        # 核心修改：将原本具体的 @v3.x.x 替换为 @master 
        # 直接使用该插件主分支的最新稳定代码，完美解决版本和 Node 兼容问题
        uses: aormsby/Fork-Sync-With-Upstream-action@master
        with:
          target_repo_token: ${{ secrets.GITHUB_TOKEN }}
          upstream_sync_repo: xianxie6/build-industry-knowledge-system
          target_sync_branch: main
          upstream_sync_branch: main
          upstream_pull_args: --rebase
